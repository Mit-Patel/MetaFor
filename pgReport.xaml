<Page x:Class="MetaFor_Demo.pgReport"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      Background="{StaticResource PageBgColor}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="pgReport">

    <Grid>
        <StackPanel>
            <Border Background="WhiteSmoke" Margin="10 20" CornerRadius="5">
                <StackPanel>
                    <Grid x:Name="gridMain" Margin="25" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30*" />
                            <ColumnDefinition Width="70*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                        </Grid.RowDefinitions>
                    </Grid>
                    <Button x:Name="btnExport" Content="Export to Excel" Foreground="{StaticResource MenuItemFgColor}" Width="200" Click="btnExport_Click" />
                    <Button x:Name="btnGPS" Content="Show GPS Coordinates for Images" Foreground="{StaticResource MenuItemFgColor}" Width="300" Click="btnGPS_Click" Margin="0,20,0,20" />
                </StackPanel>
            </Border>            
        </StackPanel>
    </Grid>
</Page>
